-- Entry point — loaded automatically by Neovim's plugin loader.
-- Users who want manual control can set `config.auto_setup = false`
-- and call require("startup_tips").setup({}) themselves.
if vim.g.loaded_startup_tips then
  return
end
vim.g.loaded_startup_tips = 1
